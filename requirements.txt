# Phase 0 — laptop voice loop
sounddevice>=0.4.6        # PortAudio mic capture (Windows-friendly)
numpy>=1.24
webrtcvad>=2.0.10         # if wheels are missing, install `webrtcvad-wheels` instead
faster-whisper>=1.0.0     # local STT (CTranslate2)
requests>=2.31            # Ollama HTTP client
PyYAML>=6.0
pyttsx3>=2.90             # offline TTS (Windows SAPI5)

# Phase 1+ — serial link to the ESP32
pyserial>=3.5

# dev
pytest>=8.0
