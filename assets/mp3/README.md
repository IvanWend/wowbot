# Robot audio (superseded)

This folder was going to hold TF-card sound-effect clips for an MP3 module. That path
was retired: the MAX98357A amp streams all audio from the laptop (Phase 6), so there is
no clip playback and nothing lives here. The old `SAY:`/speech-module path was removed
along with the `firmware/src/speech_controller.*` files.
